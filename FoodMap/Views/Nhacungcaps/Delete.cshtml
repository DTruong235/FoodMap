@model FoodMap.Models.Nhacungcap

@{
    ViewData["Title"] = "Xóa Nhà cung cấp";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="container py-4" style="max-width: 600px;">
    <div class="card shadow-sm border-danger border-2 rounded-3">
        <div class="card-header bg-danger text-white py-3">
            <h5 class="fw-bold mb-0">
                <i class="bi bi-exclamation-triangle-fill me-2"></i>XÁC NHẬN XÓA
            </h5>
        </div>
        <div class="card-body p-4">
            <p class="text-muted mb-4">
                Bạn có chắc chắn muốn xóa Nhà cung cấp này? Hành động này không thể hoàn tác!
            </p>

            <dl class="row mb-4">
                <dt class="col-sm-4 text-muted">Mã Vendor:</dt>
                <dd class="col-sm-8 fw-bold">#@Model.MaNcc</dd>

                <dt class="col-sm-4 text-muted">Tên Gian hàng:</dt>
                <dd class="col-sm-8 fw-bold text-danger">@Model.TenCongTy</dd>

                <dt class="col-sm-4 text-muted">Email / SĐT:</dt>
                <dd class="col-sm-8">@(Model.Email ?? Model.DienThoai ?? "—")</dd>
            </dl>

            <form asp-action="Delete" class="d-flex justify-content-between align-items-center pt-3 border-top">
                <input type="hidden" asp-for="MaNcc" />
                <a asp-action="Index" class="btn btn-outline-secondary rounded-pill btn-sm px-3">
                    <i class="bi bi-arrow-left me-1"></i> Hủy bỏ
                </a>
                <button type="submit" class="btn btn-danger rounded-pill btn-sm px-4 fw-bold shadow-sm">
                    <i class="bi bi-trash me-1"></i> Đồng ý xóa
                </button>
            </form>
        </div>
    </div>
</div>